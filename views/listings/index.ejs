<body>
  <% layout('/layouts/boilerplate.ejs') -%>
  <ul class="list-unstyled">
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-1">
      <% for (let list of AllLists) { %>
      <a href="/listings/<%= list._id %>" class="show-card-link">
        <div class="col mb-4">
          <div class="card special_card" style="width:20rem">
            <img src="<%= list.image.url %>" class="card-img-top" alt="" />
            <div class="card-body">
              <div class="card-text"><b><%= list.title %></b></div>
              <div class="card-price">
                ₹<%= list.price.toLocaleString('en-IN') %>/night &nbsp; <div class="gst">+ 18%GST</div>
              </div>
            </div> 
          </div>
        </div>
      </a>
      <% } %>
    </div>
  </ul>
  
</body>
